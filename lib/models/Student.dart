class Student{
  String id;
  String name;
  int age;
  int grade;
  String address;
  String email;
  int phone;
  double iqLevel;

  // Student();

  Student(this.id, this.name, this.age, this.grade, this.address, this.email, this.phone, this.iqLevel);
}